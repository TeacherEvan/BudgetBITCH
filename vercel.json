{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then convex deploy --cmd \"npm run build\"; else npm run build; fi",
  "crons": [
    {
      "path": "/api/cron/projections/check-ins/replay",
      "schedule": "0 2 * * *"
    }
  ]
}
